export interface IProject {
  name: string;
  id: string;
  target: string;
  url_target: string;
  release: string;
  url_release: string;
  first_build: string;
  story_points: number;
  completion: number;
}
